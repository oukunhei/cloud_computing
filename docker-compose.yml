version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: web-portal/Dockerfile
    network_mode: host
    volumes:
      - /etc/rancher/k3s/k3s.yaml:/host/kubeconfig:ro
    environment:
      - KUBECONFIG=/app/kubeconfig
      - FLASK_PORT=8080
    restart: unless-stopped
