apiVersion: v1
kind: Pod
metadata:
  name: quota-fill-a
spec:
  containers:
  - name: app
    image: nginx:alpine
    resources:
      requests:
        cpu: "1"
        memory: "2Gi"
      limits:
        cpu: "1"
        memory: "2Gi"
---
apiVersion: v1
kind: Pod
metadata:
  name: quota-fill-b
spec:
  containers:
  - name: app
    image: nginx:alpine
    resources:
      requests:
        cpu: "1"
        memory: "2Gi"
      limits:
        cpu: "1"
        memory: "2Gi"
---
apiVersion: v1
kind: Pod
metadata:
  name: quota-fill-c
spec:
  containers:
  - name: app
    image: nginx:alpine
    resources:
      requests:
        cpu: "1"
        memory: "2Gi"
      limits:
        cpu: "1"
        memory: "2Gi"
